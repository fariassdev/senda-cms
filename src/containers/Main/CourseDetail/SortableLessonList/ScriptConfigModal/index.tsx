'use client';

/**
 * ScriptConfigModal
 *
 * This is a re-export of the shared ScriptGenerationModal component.
 * Kept for backward compatibility with existing imports in CourseDetail.
 *
 * @see src/components/ScriptGenerationModal for the unified implementation
 */
export { ScriptGenerationModal as ScriptConfigModal } from '@/components/ScriptGenerationModal';
export type { ScriptGenerationModalProps as ScriptConfigModalProps } from '@/components/ScriptGenerationModal';
